1.C++：概念比较模糊

封装：安全性，
集成：
多肽：绑定，

虚函数:模糊
虚继承：
纯虚函数：
虚析构：模糊


设计模式：
单例模式：

windows能力
音频采集方式：无（安卓）
音频转换：音频转换


视频采集方式：
H264编码：少
IPC 和 RTSP :
IPcamera: 只有rtsp能力 没有sdk能力，基本局域网传输

openVC:无

openGL:ios端渲染.

libevent: 
libcurl: 报警上传,http上传

UI:MFC: 基本控件的使用。

孙守贤
tcp/udp 基本会用
网络模型: 不会

C++的三大特性：

window端的优势：
大部分的时间在linux上，
